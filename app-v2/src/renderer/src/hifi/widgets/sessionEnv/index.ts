import type { HifiWidgetModule } from '../types'

// Owned by the 'sessionEnv' group agent. Add HifiWidgetModule entries here (one per telemetry).
export const SESSION_ENV_WIDGETS: HifiWidgetModule[] = []
