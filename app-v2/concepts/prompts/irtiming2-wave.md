# irTiming2 wave prompt

Flow: build title-less transparent SVG timing widgets from the two generated refs. Deltas use a large signed digital number with small `s`, green for faster/negative and red for slower/positive, over a centre-zero segmented bar with tiny dim qualifier tags (`OPT`, `SES`, `DRV`). Estimated lap uses a cyan-white seven-segment-style `m:ss.mmm` readout above one hairline. Keep everything legible on grey/game backgrounds, NaN-safe, and panel-free.
